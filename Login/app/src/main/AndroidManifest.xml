<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    package="com.example.login">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".requisition_form1"
            android:label="@string/title_activity_requisition_form1"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".JsonSample" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".request_page"
            android:label="@string/title_activity_request_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Forgot_Password" />
        <activity android:name=".Recycler" />
    </application>

</manifest>